# Django settings
SECRET_KEY=your_secret_key_for_development_only_change_it_in_production
DEBUG=1

# Database settings
DATABASE_URL=postgres://wubi_user:wubi_password@db:5432/wubi_dict_sync_db

# PostgreSQL settings (used by the db service)
POSTGRES_DB=wubi_dict_sync_db
POSTGRES_USER=wubi_user
POSTGRES_PASSWORD=wubi_password

# Entrypoint script settings
DB_HOST=db
DB_PORT=5432
